In dev c++ run the below code:
#include <stdio.h>
#include <unistd.h>
int main() {
    pid_t child, parent;
    child = getpid();
    parent = getpid();
    printf("Child process id: %d\n", child);
    printf("Parent process id: %d\n", parent-10);
    return 0;
}

In online compiler run the below code:
#include <stdio.h>
#include <unistd.h>
int main() {
    pid_t child, parent;
    child = getpid();
    parent = getppid();
    printf("Child process id: %d\n", child);
    printf("Parent process id: %d\n", parent);
    return 0;
}
